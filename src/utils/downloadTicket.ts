export const downloadTicket = async (ticketElement: HTMLElement): Promise<string> => {
    // Implementation to generate and return the ticket image as a string (e.g., a data URL)
    const ticketImage = "data:image/png;base64,..."; // Example data URL
    return ticketImage;
};